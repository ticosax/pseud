language: python

python:
    - "2.7"

install:
    - "pip install -e .[Tornado,Gevent] . --install-option=\"--zmq=bundled\""
    - "pip install coverage coveralls pytest"

script: "coverage run --source=pseud setup.py test"

after_success:
    coveralls

notifications:
    email: false
