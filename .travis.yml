language: python

python:
    - "2.7"
    - "3.4"

install:
    - "pip install -e .[Tornado,Gevent]"
    - "pip install coverage coveralls pytest"

script: "coverage run --source=pseud setup.py test"

after_success:
    coveralls

notifications:
    email: false
